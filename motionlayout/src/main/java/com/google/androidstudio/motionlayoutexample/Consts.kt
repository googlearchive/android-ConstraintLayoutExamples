package com.google.androidstudio.motionlayoutexample

object Consts {

    // Tentative variable to make it explicit that MotionLayout#setDebugMode(2 (or above)) works
    // as the same as MotionLayout#setShowPaths(true) in the alpha2 or below
    const val SHOW_PATHS = 2
}